/**
 * Optional R3F entry — import only after `npm run install:3d`.
 *
 * Example (desktop-only):
 *   const { CommandCenterCanvas } = await import('@/components/viz/r3f/CommandCenterCanvas');
 */
export {};
