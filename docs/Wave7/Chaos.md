# Wave 7 — Chaos

Controlled injections: gateway recreate, worker recreate, DB restart, burst probe recovery. Experiments stored in `chaos_experiments` with `recovered` / `recovery_ms` / `data_loss=false` when volumes preserved.
