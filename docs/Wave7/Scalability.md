# Wave 7 — Scalability

`GET /admin/system/certification/scalability` counts live CMDB CIs, topology edges, metric samples, traces, connectors, KG entities, and incidents — evidence of scale against production data.
