# Wave 7 — Security

Matrix covers password/session policies, secret rotation jobs, certificates, active sessions, `AUTHZ_ENFORCE`, TLS at edge, unauthenticated denial, and audit endpoint checks executed by the certification runner.
