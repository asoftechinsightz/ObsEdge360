# Phase 5 Wave 1 — Deployment

1. Deploy via `scripts/vps-deploy-latest.sh`
2. Confirm migration `033_enterprise_ga.sql`
3. Recreate api-gateway + web
4. Run `scripts/vps-p5-wave1-validate.sh`
5. Expect `P5_WAVE1_VALIDATION_OK`
