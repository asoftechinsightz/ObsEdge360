# Wave 9 — Release Lifecycle

`Wave build → validate → tag → RC (v1.0.0-rc1) → GA (v1.0.0) → maintenance patches`

Rollback: prior tags (`v1.0.0-rc1`, `v1.0.0-wave7`) with additive migration awareness.
