# Wave 9 — Version Policy

- GA line: `v1.0.x` semver patch for fixes
- Minor `v1.y.0` for compatible enhancements
- Major `v2.0.0` for breaking changes (EAB required)
- RC tags (`v1.0.0-rcN`) are not supported for production beyond pilot windows
