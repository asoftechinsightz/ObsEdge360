{{- if and .Values.secret.enabled .Values.secret.createPlaceholder }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-secrets
  labels:
    app: opsedge360
type: Opaque
stringData:
  # Placeholder only — replace via sealed-secrets / external-secrets operator
  JWT_SECRET: "REPLACE_ME"
  POSTGRES_PASSWORD: "REPLACE_ME"
{{- end }}
